/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.imgur.com', 'support.discord.com'], // Füge hier die benötigten Domains hinzu
  },
};

module.exports = nextConfig;

